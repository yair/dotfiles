# ~/.profile — POSIX login shell (bash, dash, sh -l, ...).
# Sources the shared environment, then defers to .bashrc for interactive bash.

[ -r "$HOME/.config/shell/env.sh" ] && . "$HOME/.config/shell/env.sh"

if [ -n "$BASH_VERSION" ] && [ -r "$HOME/.bashrc" ]; then
    . "$HOME/.bashrc"
fi
